import type { Metadata } from 'next'

export const metadata: Metadata = {
  description:
    'Get in touch with Gurukrupa Enterprise. Start a project, book a consultation, or discuss a SWAS delivery plan. We respond within 24 hours.',
}

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children
}
