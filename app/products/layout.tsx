import type { Metadata } from 'next'

export const metadata: Metadata = {
  description:
    "Explore Gurukrupa Enterprise's productized software: Go Digital Chat, Mod GST, Follow-up.io, and more — built for modern Indian business operations.",
}

export default function ProductsLayout({ children }: { children: React.ReactNode }) {
  return children
}
