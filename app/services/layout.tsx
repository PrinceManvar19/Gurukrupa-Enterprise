import type { Metadata } from 'next'

export const metadata: Metadata = {
  description:
    'Web development, mobile apps, AI automation, and enterprise software — delivered with hands-on service and long-term support by Gurukrupa Enterprise.',
}

export default function ServicesLayout({ children }: { children: React.ReactNode }) {
  return children
}
