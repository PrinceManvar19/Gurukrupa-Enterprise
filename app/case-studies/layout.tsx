import type { Metadata } from 'next'

export const metadata: Metadata = {
  description:
    'Real outcomes from real builds — see how Gurukrupa Enterprise has helped businesses with ERP, POS, inventory, and workflow automation.',
}

export default function CaseStudiesLayout({ children }: { children: React.ReactNode }) {
  return children
}
